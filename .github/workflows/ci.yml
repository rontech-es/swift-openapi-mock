name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Swift Tests
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: swift test
